server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/x-javascript application/xml+rss application/json;

    # API proxy - MUST be BEFORE the SPA routing location block
    # Proxies all /api/* requests to the backend service
    # For Coolify: Use the backend's external domain or internal service name
    # Option 1: External domain (recommended for production)
    location /api {
        proxy_pass https://api.track.greenbritain.club;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Important: Don't rewrite the /api prefix when proxying
        proxy_redirect off;
    }

    # SPA routing - MUST be AFTER the API proxy
    location / {
        try_files $uri $uri/ /index.html;
    }
}

